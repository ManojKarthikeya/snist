import { StyleSheet } from "react-native"

const fonts = StyleSheet.create({
    h1 : {
        fontWeight: "800",
        fontSize: 35
    },

    h3 : {
        fontSize: 25,
        fontWeight: "400"
    },
})

export default fonts